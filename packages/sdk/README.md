# @alphadb/sdk

Shared AlphaDB backend client for market reads, user state, and market streaming.

The client supports:

- normalized market reads
- backend user-state operations
- backend SSE streaming
- bearer-token auth via `apiToken`
