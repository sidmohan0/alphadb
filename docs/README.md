# Docs

This worktree follows the docs-first setup requested for the project:

- start from an empty orphan worktree
- keep `AGENTS.md` intentionally short
- store project decisions and plans in `docs/`
- keep the app independently bootable from this worktree

## Current Documents

- `docs/plans/001-polymarket-ansi-tui.md`
